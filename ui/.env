# Set to "false" to talk to the real API server instead of mock data
VITE_MOCK_MODE=true

# Base URL for the FlowCD API server (used when VITE_MOCK_MODE=false)
VITE_API_BASE_URL=http://localhost:8090
